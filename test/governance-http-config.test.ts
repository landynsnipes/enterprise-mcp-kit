import assert from 'node:assert/strict'; import test from 'node:test'; import { GovernanceValidationError } from '../src/governance.js'; import { parseGovernanceHttpConfig } from '../src/governance-http-config.js';
test('requires explicit HTTPS OIDC and bounded HTTP configuration', () => { const config = parseGovernanceHttpConfig({ GOVERNANCE_OIDC_ISSUER: 'https://issuer.example', GOVERNANCE_OIDC_JWKS_URL: 'https://issuer.example/keys', GOVERNANCE_OIDC_AUDIENCE: 'enterprise-mcp-kit', GOVERNANCE_STORAGE_PATH: '/tmp/governance.json', GOVERNANCE_HTTP_PORT: '8788' }); assert.equal(config.port, 8788); assert.equal(config.executionEnabled, false); assert.throws(() => parseGovernanceHttpConfig({ GOVERNANCE_OIDC_ISSUER: 'http://issuer.example', GOVERNANCE_OIDC_JWKS_URL: 'https://issuer.example/keys', GOVERNANCE_OIDC_AUDIENCE: 'x', GOVERNANCE_STORAGE_PATH: '/tmp/x' }), GovernanceValidationError); });
test('write execution is fail-closed and requires a separate explicit NetBox endpoint and token', () => { const base = { GOVERNANCE_OIDC_ISSUER: 'https://issuer.example', GOVERNANCE_OIDC_JWKS_URL: 'https://issuer.example/keys', GOVERNANCE_OIDC_AUDIENCE: 'enterprise-mcp-kit', GOVERNANCE_STORAGE_PATH: '/tmp/governance.json', GOVERNANCE_EXECUTION_ENABLED: 'true' }; assert.throws(() => parseGovernanceHttpConfig(base), GovernanceValidationError); const config = parseGovernanceHttpConfig({ ...base, NETBOX_WRITE_BASE_URL: 'https://netbox.example', NETBOX_WRITE_TOKEN: 'dedicated-token' }); assert.equal(config.executionEnabled, true); assert.equal(config.netboxWriteBaseUrl, 'https://netbox.example'); });
