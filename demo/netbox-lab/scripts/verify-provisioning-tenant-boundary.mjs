import{NetBoxSiteProvisioningAdapter,NetBoxProvisioningError}from'../../../dist/src/netbox-site-provisioning-adapter.js';
const adapter=new NetBoxSiteProvisioningAdapter({baseUrl:process.env.NETBOX_PROVISION_BASE_URL,token:process.env.NETBOX_PROVISION_TOKEN,timeoutMs:Number(process.env.NETBOX_PROVISION_TIMEOUT_MS??'5000')});
if(!await adapter.tenantExists('summit-digital'))throw new Error('Summit tenant reference is unavailable.');let denied=false;try{await adapter.createSite({tenantSlug:'summit-digital',name:'Unauthorized Summit Verification',slug:'unauthorized-summit-verification',facility:'DENY',physicalAddress:'Must not be created',timeZone:'UTC'});}catch(error){denied=error instanceof NetBoxProvisioningError&&error.message==='NetBox provisioning request was not authorized.';}if(!denied)throw new Error('Cross-tenant provisioning was not denied.');if(await adapter.siteExists('Unauthorized Summit Verification','unauthorized-summit-verification'))throw new Error('Cross-tenant verification record was unexpectedly created.');console.log(JSON.stringify({tenant_constrained_netbox_permission:'passed',actor_tenant:'northstar-financial',denied_tenant:'summit-digital',created:false}));
